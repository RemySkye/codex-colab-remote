---
interface:
  display_name: "Operate Colab Remote"
  short_description: "Run monitored Colab work with optional SSH"
  default_prompt: "Use $operate-colab-remote to run this work on my authorized Google Colab runtime, optionally use secure SSH, monitor it, and clean up safely."

dependencies:
  tools:
    - type: "mcp"
      value: "colab-remote"
      description: "Typed Google Colab CLI and optional SSH tools"
