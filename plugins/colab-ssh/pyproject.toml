[project]
name = "codex-colab-ssh"
version = "0.1.0"
description = "Local MCP controller for authorized Google Colab SSH sessions"
requires-python = ">=3.11"
dependencies = ["mcp>=1.20,<2"]

[tool.uv]
package = false
