# Security

Colab OAuth credentials stay in the official CLI's storage and are never parsed or returned by the plugin. Local files are denied unless their folder is explicitly approved. SSH is disabled by default.

Report vulnerabilities privately through GitHub Security Advisories. Read the full [security policy](https://github.com/RemySkye/codex-colab-remote/blob/main/SECURITY.md).
