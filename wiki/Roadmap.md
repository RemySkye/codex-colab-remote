# Roadmap

Google Cloud Storage (GCS) bucket integration is planned for future work. It will require a separate reviewed design for Google Cloud projects, billing, IAM, authentication, resumable transfers, and checkpoint synchronization.

The current release uses the protected Google Drive folder `MyDrive/codex-colab` for persistent storage.
